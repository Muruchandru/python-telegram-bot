import random
from telegram import Update
from telegram.ext import Updater, CommandHandler, MessageHandler, Filters

TOKEN = "8050137287:AAEoX1cbQZyOQ5DQM98j 6HIikdAlzhVvVSs"  # From BotFather

def start(update, context):
    update.message.reply_text('Send /predict to get 4D numbers!')

def predict(update, context):
    numbers = [
        f"{random.randint(1000, 9999)}",
        f"{random.randint(1000, 9999)}",
        f"{random.randint(1000, 9999)}"
    ]
    update.message.reply_text(f"Today's predictions:\n" + "\n".join(numbers))

updater = Updater(TOKEN)
updater.dispatcher.add_handler(CommandHandler('start', start))
updater.dispatcher.add_handler(CommandHandler('predict', predict))
updater.start_polling()
